# Third-party gitlinks

This directory contains only the four audited Git submodules listed in `docs/dependencies.md`.
Their repository URLs and commit SHAs are unchanged from the baseline; only their paths moved from
`dependencies/` to `third_party/`. `ros_gz_bridge` is installed through ROS and must not be added
here.
